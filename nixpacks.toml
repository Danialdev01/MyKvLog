[phases.setup]
nixPkgs = []

[start]
cmd = "chmod -R 777 /app/storage /app/bootstrap/cache && php artisan config:clear && node /assets/scripts/prestart.mjs /assets/nginx.template.conf /nginx.conf && (php-fpm -y /assets/php-fpm.conf & nginx -c /nginx.conf)"